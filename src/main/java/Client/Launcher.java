package Client;

import Client.Controller.UILogin;
import javafx.application.Application;

public class Launcher {
    public static void main(String[] args) {
        Application.launch(UILogin.class, args);
    }
}